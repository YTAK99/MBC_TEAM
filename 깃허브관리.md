# 1. main 최신화
- git checkout develop
- git pull origin develop

# 2. 내 브랜치 이동
- git checkout 내브랜치이름

# 3. main 내용 내 브랜치에 반영
- git merge develop

# 4. 작업
- 코드 수정

# 5. 작업 후 커밋
- git add .
- git commit -m "변경 사항"

# 6. 내 브랜치 푸시(원격 저장소에 업로드)
- git push origin 내브랜치이름

---------------------------------------------------------------------------
---------------------------------------------------------------------------

# ※ 당분간 계속 내 브랜치에서만 작업할 예정이라면 ※
- git checkout 내브랜치이름
- 코드 수정
- git add .
- git commit -m "변경 사항"

---------------------------------------------------------------------------
---------------------------------------------------------------------------

# GitHub에서 Pull Request 생성

# 1. Compare & pull request 버튼 눌러서 develop 브랜치로 merge 요청
base: develop
compare: 내브랜치이름

# 2. Able to merge 정상으로 뜨는지 확인

# 3. develop 브랜치에 merge
Create pull request
↓
Merge pull request
↓
Confirm merge


---------------------------------------------------------------------------
---------------------------------------------------------------------------


# 원격 브랜치 전체 보기
- git ls-remote --heads origin

# git branch 삭제
- git branch -d 삭제할브랜치        -> -D 하면 강제삭제


# 내 브랜치만 당겨오기
- git clone -b youngtak --single-branch https://github.com/YTAK99/MBC_TEAM.git
- git branch 로 확인


# 다른 사람 브랜치랑 merge 하기
- git fetch origin
- 1. 현재 youngtak 브랜치인지 확인 (아니라면 git checkout youngtak)
- git branch
- 2. 지환님 브랜치를 내 브랜치로 머지 시도
- git merge origin/jihwan

